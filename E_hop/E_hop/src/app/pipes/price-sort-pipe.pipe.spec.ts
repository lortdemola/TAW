import { PriceSortPipePipe } from './price-sort-pipe.pipe';

describe('PriceSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
