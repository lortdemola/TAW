import { ShakeOnHoverDirective } from './shake-on-hover.directive';

describe('ShakeOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ShakeOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
