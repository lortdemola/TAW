import { ButtonHoverAnimationDirective } from './button-hover-animation.directive';

describe('ButtonHoverAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonHoverAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
